# Phase 0 — pinned core dependencies
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.6.1
pydantic[email]==2.13.4
python-dotenv==1.0.1
httpx==0.28.1
pytest==9.0.3
pytest-asyncio==1.4.0
respx==0.23.1
email-validator==2.3.0

# Phase 1 — Data Layer & Authentication
supabase==2.31.0
redis==8.0.0
psycopg[binary]==3.2.13
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.32
# Note: Removed direct pins for postgrest, realtime, gotrue, storage3, supafunc to allow supabase 2.31.0 and httpx 0.28.1 to resolve cleanly.

# Phase 2 — MCP Client Middleware
jsonschema==4.23.0
tenacity==9.1.4
backoff==2.2.1

# Phase 3 — Core Agent Pipeline
langgraph==1.2.4
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
langchain==1.3.4
langchain-core==1.4.1
langchain-protocol==0.0.16
langchain-groq==1.1.2
langchain-google-genai==4.2.4
langsmith==0.8.9
sse-starlette==3.4.4

# Phase 4 — Memory, Personalization & Self-Correcting Loop
mem0ai==2.0.4
asyncpg==0.31.0
qdrant-client==1.18.0
grpcio==1.81.0
numpy==2.4.6
SQLAlchemy==2.0.50
protobuf==6.33.6
openai==2.41.0
posthog==7.18.0
pytz==2026.2
greenlet==3.5.1
portalocker==3.2.0
tqdm==4.68.1

# Phase 5 — Voice Pipeline (STT + TTS + VAD)
# STT: Groq Whisper API (no local model weights — uses existing groq_api_key)
webrtcvad==2.0.10
# httpx already pinned above (Phase 0)

# Phase 6 — PDF Generation & output
reportlab>=4.2.5

# Additional dependencies (utilities, serialization, etc.)
orjson==3.11.9
ormsgpack==1.12.2
filetype==1.2.0
click==8.4.1
referencing==0.37.0
websockets==15.0.1
pillow==11.3.0
